In this project i created a simple calculator with an GUI in Java.

To start the calculator, simply run the main file.

The functions are self explanatory as they are the same on a physical calculator.

